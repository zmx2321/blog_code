# Day 7：整理与验收（约 2 小时）

## 今日目标

- 目录清晰：接口在 `app/api/`，数据层在 `lib/`，页面在 `app/`，组件可放 `components/`。
- 能口头或书面说清：从点「列表」到「删掉一篇」整条链路（页面 → fetch → 哪个 route.js → 哪个 store 函数）。
- 可选：部署到 Vercel，把链接贴出来当作品。

## 在 Demo 里对应哪里

- 整体结构见仓库根目录和 `LEARN_PLAN.md` 里的「目录结构」。
- 接口清单见 `README.md`。
- 走一遍：首页 → 文章列表 → 写文章 → 提交 → 列表出现 → 点进详情 → 编辑 → 保存 → 删除 → 回列表。

## 验收清单（自测）

- [ ] 能起项目：`npm install`、`npm run dev`，无报错。
- [ ] 至少 5 个接口：list / get one / create / update / delete，用内存数组即可。
- [ ] 至少 2 个页面：列表 + 详情（或列表 + 新增），且真实调自己的 API。
- [ ] 能说清：某个按钮点了以后，请求发到哪个 URL、哪个文件处理、数据存在哪。

## 自己动手做

1. 把你在 Day 4 / Day 5 写的「待办」项目（或文章 Demo）整理一下：重命名、加一点注释、在 README 里写「怎么跑、有哪些接口」。
2. 可选：`npm run build` 通过后，用 Vercel 部署（连 GitHub 再 import 项目即可）。

## 小结

- 一周目标：会路由、会 Server/Client、会动态路由、会写接口、会前端调接口并走通 CRUD。
- 接下来可扩展：加 TypeScript、连真实数据库、加登录等。
